import { FC } from 'react';
import styles from './CalendarPanel.module.scss';

export const CalendarPanel: FC = () => (
  // const [value, onChange] = useState(new Date());

  <div className={styles.container}>Календар</div>
);
